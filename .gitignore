# Secrets & local config
.env
.env.*
!.env.example
samconfig.toml
*.pem
*.key
credentials.json

# Build / runtime artifacts
node_modules/
.aws-sam/
response.json
/tmp/
*.log
.DS_Store

# Resumes stay local / Lambda-only — do not publish
resumes/*
!resumes/.gitkeep
